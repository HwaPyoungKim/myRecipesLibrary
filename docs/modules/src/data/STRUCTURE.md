# STRUCTURE

## Responsabilidad

Definir datos, tipos y consultas simples del catálogo.

## Estructura

```text
src/data/
└─ recipes.ts
```

## Contenido de `recipes.ts`

- Imports de imágenes desde `assets`.
- Tipos `Difficulty`, `Category` y `Recipe`.
- Arrays `categories`, `difficulties` y `recipes`.
- Helpers `getRecipeById` y `getRelatedRecipes`.
